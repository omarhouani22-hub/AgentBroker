services:
  - type: web
    name: agentbroker
    runtime: python
    plan: free
    buildCommand: bash scripts/build_hermes.sh
    startCommand: gunicorn --workers 1 --threads 1 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.14
