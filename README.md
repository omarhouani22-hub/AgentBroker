# AgentBroker — Render deployment bundle

Files are ready for a Render Python Web Service.

Build command:
`pip install -r requirements.txt`

Start command:
`gunicorn app:app`

Health check:
`/health`

Payments and wallets are disabled.
